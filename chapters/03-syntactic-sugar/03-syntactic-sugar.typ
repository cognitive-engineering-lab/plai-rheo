#import "/prelude.typ": *
#show: book-style
#set document(title: [Syntactic Sugar])

#part[Syntactic Sugar]

#emph[We have now seen the essence of a small core language. In practice, programming languages need to also be #text(style: "normal")[usable]. To do this, they have to provide features that make programming convenient. We will see they can do this while minimizing their effort and pain.]
